#endpoint: ${env:PRISMA_ENDPOINT}
#endpoint: https://eu1.prisma.sh/carlo-09c9b6/recipe/dev/backend/dev
endpoint: https://recipe-app-prod-24c0ecfd5a.herokuapp.com/recipe-app-prod/prod
datamodel: datamodel.graphql
secret: ${env:PRISMA_SECRET}
hooks:
  post-deploy:
      - graphql get-schema -p prisma